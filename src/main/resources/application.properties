# =======================================
# CONFIGURA��O DO BANCO DE DADOS (DATASOURCE)
# =======================================
# URL de conex�o JDBC para o MySQL. Altere 'api_estoque' se quiser outro nome para o banco.
spring.datasource.url=jdbc:mysql://localhost:3306/api_estoque10?createDatabaseIfNotExist=true

# Usu�rio do seu banco de dados MySQL (o padr�o geralmente � 'root')
spring.datasource.username=${DB_USER_ESTOQUE}

# Senha do seu banco de dados MySQL
spring.datasource.password=${DB_PASS_ESTOQUE}

# =======================================
# CONFIGURA��O DO JPA/HIBERNATE
# =======================================
# Diz ao Hibernate para n�o criar ou alterar tabelas. Vamos usar o Flyway para isso.
spring.jpa.hibernate.ddl-auto=validate

# Mostra no console os comandos SQL que o JPA est� executando. �timo para debug.(DESLIGADO)
spring.jpa.show-sql=false

# Formata o SQL exibido no console para ficar mais leg�vel.(DESLIGADO)
spring.jpa.properties.hibernate.format_sql=false

# =======================================
# CONFIGURA��O DO FLYWAY
# =======================================
# Habilita o Flyway
spring.flyway.enabled=true

# Chave secreta para a assinatura dos tokens JWT
api.security.token.secret=${JWT_SECRET_ESTOQUE}

api.security.super-admin.id=1

server.address=0.0.0.0

